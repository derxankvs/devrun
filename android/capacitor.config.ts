import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devrun.app',
  appName: 'DevRun',
  webDir: 'devrun-app.netlify.app'
};

export default config;
