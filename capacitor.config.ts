const config = {
  appId: 'com.devrun.app',       // ID do app
  appName: 'DevRun',             // Nome do app
  webDir: 'out',                 // Pasta onde seu app Next.js será exportado
  bundledWebRuntime: false
};

export default config;
